# Development and CI dependencies.
# ruamel.yaml is also required by the workbench tests and config-writing code.
ruamel.yaml>=0.18
ruff>=0.9
pytest>=8
bandit[toml]>=1.8
pip-audit>=2.10.1
pre-commit>=4
